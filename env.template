# Database Configuration
# Railway: set DATABASE_URL (e.g. from MySQL.MYSQL_URL). Local/Docker: use DATABASE_URL or DB_* below.
DATABASE_URL=
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_NAME=wallet_db

# Application (Railway: NODE_ENV=production)
NODE_ENV=development
PORT=3000
